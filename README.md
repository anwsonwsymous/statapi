# Endpoints

- `/api/get` - returns all countries with their visit counts
- `/api/update/{countryCode}` - increments visit count

# Setup

- Copy `.env.example`

- Start application

```shell
./app.sh --fresh
```
